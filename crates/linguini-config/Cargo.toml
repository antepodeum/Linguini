[package]
name = "linguini-config"
version = "0.1.0-alpha.4"
description = "Configuration parsing and file discovery for Linguini projects"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.8.23"

[dev-dependencies]
serde_json = "1.0.149"
tempfile = "=3.10.1"
