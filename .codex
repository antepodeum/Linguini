Repository state handoff - 2026-05-12

Completed slice:
- Added locale `override` syntax parsing for explicitly replacing parent scope declarations.
- Added locale scope loader for root-to-child source chains, registering local enums, functions, forms, standalone messages, and grouped messages.
- Added duplicate declaration and invalid shadowing diagnostics for locale scope merging.
- Added schema symbol table in `linguini-schema` for enums, variants, type aliases, public messages, grouped messages, groups, docs, and spans.
- Added schema duplicate declaration diagnostics with related first-declaration spans.
- Added schema type-reference validation against builtins and declared schema types.
- Added analyzer diagnostic model with severity, related spans, notes, quick-fix hints, and replacement payloads.
- Added Ariadne renderer with deterministic ASCII/no-color output and byte-span indexing.
- Wired `linguini check` to parse discovered schema and locale files and return rendered syntax diagnostics when parsing fails.
- Added CLI diagnostic output test and committed diagnostic golden snapshot.
- Added Chumsky 0.13.0-based lexer foundation in `linguini-syntax`, including token/span model, lexer recovery, schema-specific lexing, and committed token snapshots.
- Added schema parser AST and `parse_schema` entry point for enums, custom scalar aliases, schema formatter annotations, message signatures, grouped messages, and doc comments.
- Added locale parser AST and `parse_locale` entry point for local enums, forms, selector maps, plural-map-shaped branches, local functions, public/grouped message implementations, placeholders, dotted/call expressions, and placeholder formatter annotations.
- Added recursive nested form attribute parsing for object-shaped attributes inside locale forms.
- Added parser recovery entry points for schema and locale parsing. They return `ParseOutput<T>` with optional AST plus collected lex and parse diagnostics.
- Added invalid schema and locale fixtures that assert diagnostics are produced while strict parser APIs still fail.
- Expanded golden schema and Russian locale fixtures from minimal examples to spec-shaped fixtures covering docs, formatter annotations, forms, selector maps, plural-shaped maps, nested form attributes, helper functions, placeholders, and grouped messages.
- Updated parser and fixture checklist items through parser recovery and invalid fixture diagnostics.

Implementation decisions:
- Locale scope loading takes an ordered path/source list; `linguini-config` remains responsible for computing the top-down path chain.
- Locale grouped messages are registered both by group name and `group.message` key.
- `override` wraps the inner locale declaration in the syntax AST and transfers doc comments to that inner declaration.
- Schema symbol table stores grouped messages under `group.message` keys and group metadata separately.
- Builtin schema types currently recognized by symbol resolution: `String`, `Number`, `Decimal`, `Date`, and `Boolean`.
- Diagnostic rendering lives in `linguini-analyzer` so syntax and future semantic checks share one model.
- CLI syntax diagnostics convert parser recovery errors into analyzer diagnostics with a syntax-error note.
- Ariadne renderer uses ASCII and disabled color to keep tests stable across environments.
- Lexer uses `chumsky = "0.13.0"`.
- Public spans are byte offsets.
- Raw text after `=` and `=>` currently includes leading spaces, matching "text after delimiter" literally.
- `lex_schema` disables raw-text transition after `=` because schema custom scalar aliases use code syntax after equals.
- Mode transitions are coordinated in Rust, while token recognition is done with Chumsky parsers.
- `TokenKind::Error` represents recovered invalid source characters.
- Placeholder mode tokenizes code punctuation without letting `=` switch into raw text.
- Multiline raw text currently treats `"` as a delimiter boundary so triple quotes can be recognized; escaped/ordinary quotes in raw text are deferred.
- Parser trivia stripping removes whitespace, newlines, and ordinary comments, but preserves doc comments as AST nodes.
- Recovered parser APIs use recovered lexing, drop `TokenKind::Error` tokens, then preserve all lex and parse errors in order.
- Schema parser is syntax-only and does not resolve type names or formatter semantics.
- Locale parser is syntax-only and does not resolve schema messages, form variants, selector enums, CLDR plural categories, formatter compatibility, or function arity.
- Locale `name = value` attributes are parsed as text patterns because the lexer intentionally enters raw-text mode after `=`; scalar-vs-text meaning is deferred to semantic analysis.
- Form map bodies cover branch maps used by selector and plural-shaped examples, and recursive object-shaped nested attributes used by forms.

Tests last run:
- `cargo test -p linguini-syntax -p linguini-locale`
- `cargo fmt --all --check`
- `./scripts/check-file-size.sh` (warned that `crates/linguini-syntax/src/lib.rs` has 484 lines, `crates/linguini-syntax/src/lexer.rs` has 426 lines, and `crates/linguini-syntax/src/parser/locale_parser.rs` has 408 lines; all below 500-line failure threshold)
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo test -p linguini-schema`
- `cargo test -p linguini-analyzer -p linguini-cli`
- `cargo fmt --all`
- `cargo fmt --all --check`
- `./scripts/check-file-size.sh` (warned that `crates/linguini-syntax/src/lib.rs` has 469 lines and `crates/linguini-syntax/src/lexer.rs` has 426 lines; both below 500-line failure threshold)
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo test -p linguini-syntax`
- `cargo test -p linguini-syntax` after fixture expansion and nested form attribute parsing.
- `wc -l crates/linguini-syntax/src/parser.rs crates/linguini-syntax/src/parser/locale_parser.rs crates/linguini-syntax/src/ast.rs crates/linguini-syntax/src/lib.rs`
- `cargo fmt --all --check`
- `./scripts/check-file-size.sh` (warned that `crates/linguini-syntax/src/lib.rs` has 443 lines and `crates/linguini-syntax/src/lexer.rs` has 422 lines; both below 500-line failure threshold)
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo test -p linguini-syntax` after parser recovery and invalid fixture diagnostics.

Known deferred work:
- Locale scope symbols currently store declaration metadata only; variant/function/form internals remain for analyzer-specific checks.
- Schema symbol table currently validates one parsed schema AST at a time; project-wide namespace merging remains future analyzer work.
- Raw text quote handling should be revisited when parser fixtures cover quoted localized text.
- Parser recovery currently reports diagnostics and optional full AST output; it does not insert error AST nodes for malformed declarations.

Next recommended task:
- Begin analyzer checks: match locale messages to schema messages, report missing/unknown public messages, then form and branch coverage checks.
